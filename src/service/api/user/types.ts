

export interface UserInfoData {
  userId: number,
  username: string,
  email: string,
  avatar: string,
  bio: string,
  theme: string,
  chatCount: number,
  memoirCount: number,
  useDay: number
}